import secrets

def gerar_chave_privada():
    # Gera um número aleatório de 256 bits
    chave_privada = secrets.token_hex(32)  # 32 bytes = 256 bits
    return chave_privada

if __name__ == "__main__":
    chave_privada = gerar_chave_privada()
    print(f"Sua chave privada é: {chave_privada}")
